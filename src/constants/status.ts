export enum StatusType {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum CronjobStatusType {
  DONE = 'done',
  FAILED = 'failed',
}

export enum PaymentStatus {
  NOT_PAY = 'not-pay',
  PAY_PART = 'pay-part',
  PAID = 'paid',
}